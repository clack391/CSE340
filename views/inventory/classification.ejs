

<h1><%= (typeof title !== 'undefined' && title) ? title : 'Vehicles' %></h1>

<% if (typeof grid !== 'undefined' && grid) { %>
  <%- grid %>
<% } else { %>
  <p class="notice">Sorry, no matching vehicles could be found.</p>
<% } %>
